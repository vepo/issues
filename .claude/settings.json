{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *\"/generated/\"*) exit 0 ;; *\"/src/main/webui/\"*.ts) (cd src/main/webui && ./node_modules/.bin/eslint --fix \"$f\") ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
