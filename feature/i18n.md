# UI internationalization (i18n)

**Feature version:** 1  
**Status:** done  
**Requested:** 2026-07-11

## Summary

Ship **UI internationalization** so Issues presents chrome, labels, and **system labels** in **English** or **Portuguese**, with **`pt` as the Angular source locale**. Language is chosen in **account settings** and stored on the **user** profile; unauthenticated visits follow the **browser** `Accept-Language`. Locale URLs use path prefixes **`/pt/`** and **`/en/`**. Preference is **seeded on register / first provisioning**. API errors and emails stay as today. User-authored content is never auto-translated.

Backlog: [docs/backlog.md](../docs/backlog.md). Deferred earlier from [ui-design-system.md](ui-design-system.md) Q3.

## Scope (locked by answered FQs)

### In scope (v1)

| Id | Capability | Source |
|----|------------|--------|
| S1 | Locales **`pt`** (source) + **`en`** | **FQ1** |
| S2 | UI-only: SPA chrome, static copy, `$localize` / `i18n` markers | **FQ2**, **FQ6** |
| S3 | Language select on **`/account/settings` only** | **FQ3** |
| S4 | **UI locale** persisted on **User** (server) | **FQ4** |
| S5 | Unauthenticated locale from **browser** `Accept-Language` → `pt` \| `en`, else `pt` | **FQ5** |
| S6 | Seed **locale preference** on register and LDAP/ENDPOINT first provisioning | **FQ11** |
| S7 | **System labels** follow active UI locale | **FQ7** |
| S8 | Full EN parity for all screens (templates + TS + B6 gaps) | **FQ9** |
| S9 | Path-prefix locale routing `/pt/…`, `/en/…` | **AQ2** |
| S10 | Dev: single active locale via config toggle; CI builds both | **AQ3** |
| S11 | Date/number formatting + Material locale data per build | S1 |

### Out of scope (v1)

| Item | Source |
|------|--------|
| API error / validation message localization | **FQ2**, **FQ6** |
| Email body localization | **FQ6** |
| Locales beyond `pt` / `en` | **FQ1** |
| Translating user-authored strings | **FQ8** |
| Header or login language switcher | **FQ3** |
| Bilingual product docs — stay PT-BR | **FQ10** |
| Runtime `ngx-translate` | **AQ1** |

### Ubiquitous language (synced to domain-spec)

| Term | Meaning | Avoid |
|------|---------|-------|
| **UI locale** | Active language for chrome, system labels, and formatting (`pt` or `en`) | Translating ticket body |
| **Source locale** | Template authoring language — `pt` | Treating EN as source |
| **Locale preference** | User’s stored UI locale on the server | Browser-only after seeded/saved |
| **System label** | Product-owned display string for enums and fixed UI copy | Admin-authored status / CF names |

## Feasibility (exploration)

| Building block | Status | Relevance |
|----------------|--------|-----------|
| `sourceLocale: "pt"`, `@angular/localize`, `extract-i18n` | Shipped | Add `en` + XLF |
| Template `i18n` markers | Partial | Complete + B6 |
| Hard-coded PT in TS | Gap | Shared catalog + `$localize` |
| Dual locale build / path serve | Missing | **AQ2**, **AQ3** |
| `User` locale field | Missing | **FQ4**, **FQ11** |

## Wireframe

| Field | Value |
|-------|-------|
| **Source** | ASCII below |
| **Last updated** | 2026-07-11 (FQ11, AQ2, AQ3) |

### Screen: `/account/settings` — language control (**FQ3**, **FQ4**)

| Region | Elements | Notes |
|--------|----------|-------|
| Profile | Name, email, roles | Unchanged |
| Language | **Idioma** / **Language**; select Português (`pt`) \| English (`en`); saved with profile | After save, reload to `/pt/…` or `/en/…` (**AQ2**) |
| Password | Existing | Unchanged |

```
┌─────────────────────────────────────────────┐
│  Conta                                      │
├─────────────────────────────────────────────┤
│  Perfil …                                   │
├─────────────────────────────────────────────┤
│  Idioma                                     │
│  [ Português ▾ ]                            │
│  [ Salvar ]  → reload /{locale}/account/…   │
├─────────────────────────────────────────────┤
│  Alterar senha …                            │
└─────────────────────────────────────────────┘
```

### Unauthenticated / login (**FQ5**)

No switcher. Entry `/` negotiates `Accept-Language` → redirect to `/pt/…` or `/en/…`. After login, if preference ≠ current path locale, redirect to `/{preference}/…`.

### Header

No language menu.

## Impact

| Area | Effect |
|------|--------|
| Bounded contexts | `user`, `auth`; SPA-wide frontend |
| Packages / files | `User`, `AuthResponse`, `UpdateProfileRequest`, register/provision paths; `account-settings`; `angular.json` + `messages.en.xlf`; `system-labels.ts`; Quarkus static/`baseHref` routing; Quinoa/dev locale toggle |
| API | `locale` on `AuthResponse` + `UpdateProfileRequest`; optional/required on register body or `Accept-Language` at provision |
| UI | Account settings language; all static + system labels; path-prefixed app |
| Schema / seed | `tb_users.ui_locale VARCHAR(8) NOT NULL DEFAULT 'pt'`; `dev-import.sql` `pt` |
| Tests | Me/profile/register/provision locale; Angular settings; dual-locale CI build |
| Docs | domain-spec (updated); feature-catalog; README; gallery; ARCHITECTURE |

### Risks

- Path-prefix + Quarkus/Quinoa routing is the main delivery risk (**AQ2**, **AQ3**).
- Full EN parity (**FQ9**) is a large translation pass — plan dedicated task.
- API/email remain unlocalized while UI is EN (**FQ6**).

### Feature questions (FQ*n*)

| # | Question | Status | Answer |
|---|----------|--------|--------|
| FQ1 | Locales / BCP-47 ids? | answered | **`pt`** + **`en`** |
| FQ2 | UI-only vs API/email? | answered | **UI only** |
| FQ3 | Where to switch? | answered | **Account settings only** |
| FQ4 | Preference storage? | answered | **Server** |
| FQ5 | Default unauthenticated? | answered | **Browser** `Accept-Language` |
| FQ6 | Backend / email in v1? | answered | **Out of scope** |
| FQ7 | Translate system enums? | answered | **Yes** |
| FQ8 | User-authored untranslated? | answered | **Yes** |
| FQ9 | v1 completeness? | answered | **Full app EN parity** |
| FQ10 | Product docs language? | answered | **Remain PT-BR** |
| FQ11 | When is preference first written? | answered | **(b)** Seed from browser / active SPA locale on **register** and on **LDAP/ENDPOINT first provisioning** |

## Architecture

| Area | Design |
|------|--------|
| Bounded contexts | Identity & access owns **locale preference**; UI strings are frontend-only |
| Packages / layers | `User.uiLocale`; `AuthenticationService` register/updateProfile/me/provision; endpoints unchanged structurally |
| API | `AuthResponse.locale`; `UpdateProfileRequest.locale` (allow-list `pt`\|`en`); register/provision seeds locale from request body locale and/or `Accept-Language` (map → `pt`\|`en`, else `pt`) |
| Schema | `tb_users.ui_locale VARCHAR(8) NOT NULL DEFAULT 'pt'` in `V1.0.0`; seed personas `pt` |
| Cross-context | None |
| Frontend | Compile-time i18n; `baseHref` `/pt/` and `/en/`; shared `system-labels.ts`; account settings select; reload to other prefix on save |
| Serve | Root `/` → 302 to `/pt/` or `/en/` from `Accept-Language`; static assets under each prefix; authenticated mismatch → redirect to preference prefix |
| Dev (**AQ3**) | `quarkus:dev` / Quinoa serves **one** locale (default `pt`); toggle via npm script / env (e.g. `ISSUES_UI_LOCALE=en`); CI runs production build for **both** locales |
| Tests | Endpoint + Angular + dual-locale build smoke in CI |

### Locale resolution

```
GET /
  → Accept-Language → pt|en (else pt) → 302 /{locale}/…

SPA under /{locale}/…
  → compile-time strings for that locale

Register / first LDAP|ENDPOINT provision
  → set User.uiLocale from client locale or Accept-Language (else pt)

Login (existing user)
  → if path locale ≠ User.uiLocale → redirect to /{uiLocale}/… (keep deep link path)

Account settings save
  → POST profile with locale → reload /{newLocale}/account/settings
```

### Architecture questions (AQ*n*)

| # | Question | Status | Answer |
|---|----------|--------|--------|
| AQ1 | Compile-time vs runtime? | answered | **Compile-time Angular i18n** |
| AQ2 | URL strategy? | answered | **(a) Path prefix** `/pt/…`, `/en/…` |
| AQ3 | Quinoa / dev? | answered | **(a) Single locale in `quarkus:dev`** (config/env toggle); **CI builds both** |
| AQ4 | Server preference shape? | answered | Extend **User + AuthResponse + UpdateProfileRequest** |
| AQ5 | System labels? | answered | **Shared TS catalog** with `$localize` |

**Gate:** all blocking FQ/AQ answered → task break below.

## Changelog

### UI i18n — EN + PT — 2026-07-11

**Version:** 1  
**Status:** done

**Description:** Path-prefixed `pt`/`en` UI; account-settings language control; server `ui_locale` seeded on register/provision; full static + system-label EN parity; no API/email i18n.

**Impact on other features:**

| Feature / area | Impact |
|----------------|--------|
| [account-settings.md](account-settings.md) | Language panel; profile API `locale` |
| [ui-design-system.md](ui-design-system.md) | Closes i18n/B6; gallery language field |
| [onboarding-hints.md](onboarding-hints.md) | Localized hint copy |
| Authentication / register / LDAP | Seed `ui_locale`; post-login path redirect |
| All UI features | EN XLF + system-label catalog |
| email-delivery | None (**FQ6**) |

#### Feature checklist

| ID | Criterion | Source | Done |
|----|-----------|--------|------|
| FC1 | Locales are `pt` + `en` only | FQ1 | ☑ |
| FC2 | Language control only on `/account/settings` per **Wireframe** | FQ3 | ☑ |
| FC3 | Preference on user; unauthenticated uses browser → path redirect | FQ4, FQ5, AQ2 | ☑ |
| FC4 | Seed on register and LDAP/ENDPOINT first provision | FQ11 | ☑ |
| FC5 | Full-app static + system labels follow UI locale | FQ7, FQ9 | ☑ |
| FC6 | User-authored content not auto-translated | FQ8 | ☑ |
| FC7 | API errors and emails unchanged | FQ2, FQ6 | ☑ |
| FC8 | Path prefixes `/pt/`, `/en/`; single-locale dev + dual CI build | AQ2, AQ3 | ☑ |
| FC9 | domain-spec UI locale terms + account profile invariant | Docs | ☑ |
| FC10 | feature-catalog Account settings + README Features (PT-BR docs) | FQ10 | ☑ |
| FC11 | ui-elements-gallery documents language select | Docs | ☑ |

#### Tasks

| ID | Task | Done |
|----|------|------|
| T1 | Schema + entity: `tb_users.ui_locale NOT NULL DEFAULT 'pt'`; `User` field; `dev-import.sql` | ☑ |
| T2 | API: `AuthResponse.locale`, `UpdateProfileRequest.locale` (allow-list); update profile + me; endpoint tests | ☑ |
| T3 | Seed locale on register + LDAP/ENDPOINT first provision (client locale and/or `Accept-Language`); tests | ☑ |
| T4 | Angular i18n: `locales.en`, `baseHref` `/pt/` & `/en/`, extract pipeline, `messages.en.xlf` scaffold | ☑ |
| T5 | Serve/routing: `/` → Accept-Language redirect; static under prefixes; post-login preference redirect | ☑ |
| T6 | Dev single-locale toggle (`quarkus:dev` / npm); CI dual-locale production build check | ☑ |
| T7 | Shared `system-labels.ts` (`$localize`); migrate Priority, Phase status, Ticket type, link types, import labels | ☑ |
| T8 | Account settings language select + save + reload to `/{locale}/account/settings`; specs | ☑ |
| T9 | Full i18n pass: close B6 gaps (placeholders, aria, toggles); EN translations for all extracted units | ☑ |
| T10 | Material / `LOCALE_ID` / date-time formatting per locale build | ☑ |
| T11 | Docs: feature-catalog, README Features, ui-elements-gallery, ARCHITECTURE §13; note account-settings | ☑ |
| T12 | Finish gate: `mvn verify` + dual-locale `npm` build; Feature checklist recheck | ☑ |

#### Test coverage

| ID | Test | Covers | Done |
|----|------|--------|------|
| TC1 | `UpdateProfileEndpointTest` / `MeEndpointTest` — get/set `locale`; reject invalid | T2 | ☑ |
| TC2 | Register seeds `ui_locale`; LDAP/ENDPOINT create seeds locale | T3 | ☑ |
| TC3 | `system-labels` / component specs use catalog (priority/phase) | T7 | ☑ |
| TC4 | `account-settings.component.spec.ts` — select + save locale | T8 | ☑ |
| TC5 | Dual-locale production build succeeds in CI/script | T4, T6, T9, T12 | ☑ |
| TC6 | Manual/smoke: `/` redirect, path prefix navigation, post-login locale align | T5 | ☑ |

**Development approval:** approved 2026-07-16 — tasks: T1, T2, T3, T4, T5, T6, T7, T8, T9, T10, T11, T12

**Implementation notes:** `User.uiLocale` + `UiLocale` allow-list; register/login seed from body/`Accept-Language`; `SPARouting` redirects `/` and unprefixed paths. Angular compile-time i18n with `/pt/` `/en/` `baseHref`; `system-labels.ts` + account Idioma select; `messages.en.xlf` merged via `scripts/merge-en-xlf.js` (system/account/login IDs translated; remaining units carry PT source as target until copyedited). Dev: `ISSUES_UI_LOCALE` / `npm run start:locale`; CI: dual-locale `npm run build` + `build:check-i18n`. `mvn verify` green.
