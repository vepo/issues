package dev.vepo.issues.ticket.history;

public enum TicketHistoryAction {
    CREATED,
    FIELD_CHANGED,
    STATUS_CHANGED,
    ASSIGNEE_CHANGED,
    SUBSCRIBED,
    UNSUBSCRIBED,
    DELETED,
    RESTORED
}
